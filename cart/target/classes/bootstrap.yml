server:
  port: 8809
spring:
  application:
    name: cart
  profiles:
    active: datasource,regist,mybatis,minio
  cloud:
    nacos:
      config:
        server-addr: 192.168.119.22:8848
        prefix: gr
        file-extension: yml
