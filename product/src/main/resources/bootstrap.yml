server:
  port: 8082
spring:
  application:
    name: product
  profiles:
    active: datasource,regist,mybatis,minio
  cloud:
    nacos:
      config:
        server-addr: 192.168.119.22:8848
        prefix: gr
        file-extension: yml
